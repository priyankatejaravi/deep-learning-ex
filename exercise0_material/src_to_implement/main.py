from pattern import Checker, Circle, Spectrum

# Checkerboard example
checker = Checker(200, 20)
checker.draw()
checker.show()

# Circle example
circle = Circle(200, 40, (100, 60))
circle.draw()
circle.show()

# Spectrum example
spectrum = Spectrum(200)
spectrum.draw()
spectrum.show()
